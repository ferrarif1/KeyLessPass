# Highlights

- Exact policy-space permutation yields compliant, nonrepeating credentials.
- Single-use capabilities bind outputs to one complete credential context.
- Threshold evaluation keeps reusable derivation keys off the normal endpoint.
- Independent Root-Epoch replacement heals credentials after root disclosure.
- Ambiguous remote updates retain both reconstruction paths until resolved.

